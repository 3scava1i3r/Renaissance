{
  "mcpServers": {
    "cmc-mcp": {
      "url": "https://mcp.coinmarketcap.com/mcp",
      "headers": {
        "X-CMC-MCP-API-KEY": "${CMC_API_KEY}"
      }
    },
    "renaissance-strategy": {
      "url": "http://localhost:3000/mcp",
      "description": "Renaissance trading strategy skill — generates buy/sell/hold signals from CMC market data"
    }
  }
}